import { useRealmApp } from "../../../realm/RealmApp";

const DashboardReviewNewScopes = () => {
    const app = useRealmApp()
    const user = app.currentUser
    return(
        <div>
        </div>
    )
};
export default DashboardReviewNewScopes;
